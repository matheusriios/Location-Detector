export enum ErrorMessages {
  DUPLICATED_LOCALIZATION = 'The client and IP address IP:CLIENT_ID combination has already been localized within the past XXX minutes. Please wait before localizing again.',
}
